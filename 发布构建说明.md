# 发布构建说明

## 生成自包含发布候选

在仓库根目录使用 Windows PowerShell 执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\Publish-Release.ps1
```

默认先执行完整回归；任一步失败都会停止发布。仅在刚刚完成同一源码回归、用于重复验证发布脚本时，才可使用 `-SkipRegression`。

脚本生成：

```text
artifacts\release\UpperComInspectionInstrument2022-1.0.0-win-x64\
artifacts\release\UpperComInspectionInstrument2022-1.0.0-win-x64.zip
```

发布目录采用 `win-x64` 自包含、非单文件模式，包含 .NET 运行时和 WPF 依赖。客户电脑不需要安装 Visual Studio 或 .NET SDK，但必须保持解压后的目录结构完整。

发布目录中的 `checksums.csv` 使用 UTF-8 BOM，列出每个文件的相对路径、字节数和 SHA-256；脚本结束时还会显示整个 ZIP 的 SHA-256。每次重新构建后校验值可能变化，交付记录必须使用当次实际输出。

## 已自动检查

- 发布前完整回归通过；
- 主程序 EXE 存在；
- 自包含运行时 `coreclr.dll` 存在；
- 快速开始和版本说明已复制到 `docs`；
- `checksums.csv` 带 UTF-8 BOM；
- ZIP 能成功生成并计算 SHA-256。

## 当前不是最终安装包的原因

- 当前电脑没有 Inno Setup/WiX 等安装包编译器，尚未验证安装目录、快捷方式、升级、卸载和重装；
- EXE 当前未进行 Authenticode 代码签名，Windows 可能显示未知发布者；
- 尚未在一台没有开发环境的全新目标电脑验证 USB 转串口驱动、普通用户权限、Office/WPS 和 PDF 打印。

因此 `artifacts\release` 中的 ZIP 只能作为 V1.0 Release Candidate 或现场试运行包，不能替代最终客户安装程序。

## 数据安全

业务数据保存在当前用户 `文档\温湿度校准数据`，不放入程序安装目录。覆盖程序文件或卸载未来安装包时不得删除该目录；需要回退程序版本时，也应先备份整个数据目录。
