# 工业设备校准系统产品设计方案

## 产品定位

本软件不是多个独立工具页面的集合，而是围绕“一次校准任务”完成配置、采样、计算和归档的工业校准工作流。

核心目标：让现场人员在最少的页面切换和最少的参数输入下，完成一次可追溯的校准任务。

## 一级信息架构

```text
校准任务
  ├─ 任务配置
  ├─ 校准工作台
  ├─ 校准结果
  └─ 报告归档

历史记录
系统设置
```

当前不保留独立首页。原因是原首页只重复左侧菜单，没有提供额外状态信息。只有当首页能够展示真实的当前任务、设备报警、采样进度和最近任务时，才值得恢复。

## 页面职责

### 校准任务

只负责建立或修改任务，不负责设备采集。

配置顺序：

1. 选择校准规范和设备类型；
2. 选择校准类型；
3. 配置温湿度设定值和测点数量；
4. 配置采样次数和采样间隔；
5. 保存并进入校准工作台。

### 校准工作台

只负责执行：

- 连接巡检仪；
- 查看实时测点矩阵；
- 查看趋势曲线；
- 判断数据稳定性；
- 启动正式校准采样；
- 暂停、停止或清空本次数据。

任务参数进入工作台后只读，避免配置页和执行页出现两套参数。

### 校准结果

只展示已完成任务的计算结果：

- 示值偏差；
- 均匀度；
- 波动度；
- 不确定度；
- 合格判定。

未完成采样时禁止生成报告。

### 系统设置

维护跨任务复用的信息：

- 标准器档案；
- 证书和有效期；
- 温湿度不确定度与覆盖因子；
- 实验室环境条件；
- 串口和设备通讯默认参数。

## 交互规则

- 没有任务时不能直接进入正式校准采样；
- 没有稳定数据时不能启动校准；
- 页面切换不停止后台采集；
- 用户明确点击停止或关闭程序才停止采集；
- 校准类型改变时自动隐藏无关参数；
- JJF 1376-2012 模式隐藏湿度相关字段；
- 所有结果必须关联任务、规范、原始采样和标准器信息。

## 当前实施状态

- 主导航已按校准流程排序；
- 概览首页已移除，程序启动进入校准任务；
- 校准任务页面已简化为三步配置；
- 校准工作台已具备实时测量、稳定观察和连续校准采样基础；
- 结果页已具备采样完成状态边界；
- 结果计算、持久化和报告输出仍需继续开发。

## 最终简化架构（2026-08-18）

主导航只保留三项：

1. 校准作业；
2. 历史记录；
3. 系统设置。

“任务配置、实时采集、结果与报告”不再作为三个可以任意跳转的一级菜单，而是一次校准作业内部的三个连续步骤：

1. 任务配置：确定规范、被校设备、测点、现场环境和采样计划；
2. 实时采集与校准：连接巡检仪、观察稳定性并完成计划采样；
3. 结果与报告：查看任务依据、结果判定和报告准备状态。

关键产品约束：

- 采集运行期间点击“校准作业”直接返回正在运行的工作台；
- 采集未完成时不能进入结果步骤；
- 采集运行期间不能返回修改任务配置；
- 标准器长期资料来自系统设置，保存任务时复制为任务快照；
- 系统设置后续变更不会影响已经建立任务的标准器依据；
- 历史记录使用独立页面，不再复用当前任务报告页；
- 结果计算和报告存储未接入前，界面明确显示未完成状态，不生成虚假数据。
