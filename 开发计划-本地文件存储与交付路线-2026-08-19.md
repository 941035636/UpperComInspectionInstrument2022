# 温湿度校准系统后续开发计划

更新日期：2026-08-19
适用项目：UpperComInspectionInstrument2022
规范依据：JJF 1101-2019、JJF 1376-2012

> 说明：本文件保留本地文件存储路线和历史决策。自 2026-08-28 起，V1.0 的正式交付阶段、发布门槛、工期和验收顺序以 `正式交付开发计划-2026-08-28.md` 为准。

## 1. 当前阶段判断

当前软件已具备任务配置、巡检仪连接、实时测量、稳定条件确认、正式采样和规范结果计算能力，已经从 UI 原型进入单工况功能验证阶段。

正式校准数据落盘、历史扫描和 Excel 原始记录已经形成首个闭环。当前进入“普通实时测量可追溯、证书/PDF 输出、正式测试和现场长时间验证”阶段，后续仍围绕“任务—工况—正式样本—结果—报告”完善，不引入数据库。

## 2. 已确认的存储原则

本项目不使用 SQLite、SQL Server 或其他数据库。

所有业务数据保存为用户可直接查看、复制和备份的本地文件：

- 表格数据采用带 UTF-8 BOM 的 CSV，Excel、WPS 和 LibreOffice 可直接打开中文内容；
- 每次正式校准使用独立目录，避免不同任务的数据相互覆盖；
- 文件写入采用临时文件替换或逐行追加，尽量降低断电和异常退出造成的文件损坏；
- 任务保存时固化规范、标准器、证书、修正值、现场环境和布点快照；
- 历史记录直接扫描本地作业目录，不建立隐藏数据库索引；
- 报告可根据归档 CSV 重新生成，不依赖巡检仪在线；
- 原始数据文件不得因系统设置后续变化而被重写。

## 3. 本地作业目录结构

默认数据根目录建议使用：

`文档\温湿度校准数据`

正式校准与普通实时测量分别建立目录：

```text
温湿度校准数据
├─ JOB-20260819-143500-000_设备名称
   ├─ 作业摘要.csv
   ├─ 任务信息.csv
   ├─ 正式采样.csv
   ├─ 正式采样原始通道.csv
   ├─ 校准结果.csv
   ├─ 不确定度分量.csv
   ├─ 操作记录.csv
   └─ 报告
      ├─ 校准原始记录.xlsx
      ├─ 校准证书.docx
      └─ 校准归档.pdf
└─ 实时测量记录
   └─ RT-20260821-103300-000_设备名称
      ├─ 实时测量摘要.csv
      ├─ 实时测量.csv
      └─ 实时测量原始通道.csv
```

当前已实现正式校准六个 CSV、普通实时测量三个 CSV 和 Excel 原始记录；`操作记录.csv`、Word 证书和 PDF 归档在后续阶段接入。

## 4. 文件职责

### 4.1 作业摘要.csv

用于历史记录快速扫描，一行代表一个校准作业，至少包含：

- 数据格式版本；
- 作业编号；
- 作业状态：采样中、已中断、已完成、保存异常；
- 开始时间、完成时间；
- 规范、校准类型；
- 被校设备名称、型号、出厂编号、委托单位；
- 当前设定温度和湿度；
- 温度、湿度测点数；
- 计划样本数和已保存样本数。

### 4.2 任务信息.csv

采用“字段、值”两列方式保存任务快照，包括：

- 规范和校准点方案；
- 容积分类、工作区/测温区尺寸；
- 布点方式、中心点和布点说明；
- 设定温湿度、传感器类型、被校设备分辨力；
- 环境温度、湿度、气压和负载状态；
- 标准器名称、证书编号、有效期、型号和出厂编号；
- 标准器不确定度、覆盖因子、稳定性变化和通道修正值；
- 采样间隔、样本数、稳定依据和偏离说明。

### 4.3 正式采样.csv

一行代表一组规范正式样本，列结构为：

```text
样本序号,采样时间,被校设备温度示值,被校设备湿度示值,温度1...温度N,湿度1...湿度M
```

该文件只保存正式校准样本，不混入实时趋势刷新数据。

为兼顾日常计算和通信追溯，同时生成 `正式采样原始通道.csv`。该表每个正式通道占一行，保存修正前原始值、证书修正值、修正后测量值、有效状态、原始 HEX 和寄存器信息。测点矩阵用于办公计算，原始通道表用于核查设备通讯与解析。

### 4.4 校准结果.csv

保存结果项目、数值、单位和说明。根据规范分别输出：

- JJF 1101：温湿度偏差、均匀度、波动度和扩展不确定度；
- JJF 1376：炉温均匀度、稳定度、偏差、炉内最大温差和扩展不确定度。

### 4.5 不确定度分量.csv

每行保存一个标准不确定度分量，记录结果项目、评定点、符号、来源、A/B 类别、分布、输入量、除数、标准不确定度、灵敏系数、贡献、合成标准不确定度、包含因子和扩展不确定度。

- JJF 1101 按附录 C 展开重复性、标准器分辨力、证书修正值和标准器稳定性四项；
- JJF 1376 按附录 D 展开最高/最低点与中心监控点的重复测量平均值和证书修正值分量；
- 分量依据与合成依据写入 CSV，不只保存最终 U 值；
- 新归档数据格式版本为 1.1；1.0 历史作业生成 Excel 时明确提示原归档没有分量明细，不反推或伪造历史分量；1.1 作业缺少该文件时必须按归档不完整阻止报告生成。

### 4.6 普通实时测量记录

普通实时测量用于设备联调、稳定观察和非正式过程追溯，不参与 JJF 1101/JJF 1376 正式结果计算，也不写入正式校准作业目录。

- `实时测量摘要.csv`：保存会话起止时间、状态、串口参数、测点数、可选任务关联和已写入组数；
- `实时测量.csv`：每组完整响应占一行，按本次温湿度点数动态生成测点列，异常或缺失通道写入异常清单；
- `实时测量原始通道.csv`：逐通道保存修正前后值、有效状态、原始 HEX、寄存器地址和值；要求通道未返回时保留 `Missing` 行；
- 工作台默认开启“自动保存实时记录”，启动实时测量即建立会话，每次完整设备响应立即追加；停止、采集异常或程序关闭时更新最终状态；
- “清空数据”只清空工作台近期矩阵和曲线，不删除已经落盘的实时记录，也不结束正在进行的实时记录会话。

## 5. 领域结构演进

当前 `CalibrationTaskContext` 表示一个当前工况。经 2026-08-24 范围确认，JJF 1101-2019 与 JJF 1376-2012 没有要求软件必须在同一个作业中组织多个设定工况，因此当前产品继续采用“一次任务对应一个工况”的结构：

```text
CalibrationJob 校准作业
├─ 设备、委托、规范和标准器快照
├─ 当前工况的设定温湿度和布点
├─ 当前工况的正式样本
├─ 当前工况的计算结果
└─ 报告文件
```

同一设备需要校准多个设定点时，当前按多个独立任务分别归档，不互相覆盖。多工况汇总仅作为未来可能的效率增强，不属于当前规范符合性或交付验收范围。

## 6. 开发阶段与验收条件

### 阶段 A：建立可恢复的单工况文件归档

工作内容：

1. 新增本地文件存储服务；
2. 正式校准启动时创建作业目录和任务快照；
3. 每取得一组正式样本立即追加到 `正式采样.csv`；
4. 正式采样完成后写入 `校准结果.csv` 并更新作业状态；
5. 主动停止、关闭软件或写入失败时记录为已中断或保存异常。

验收条件：程序退出后，已经取得的正式样本仍可在 Excel/WPS 中打开；历史记录能够识别已完成和已中断任务。

### 阶段 B：接通历史记录

工作内容：

1. 扫描数据根目录下的 `作业摘要.csv`；
2. 支持按设备、编号、委托单位、规范、日期和状态过滤；
3. 支持打开作业目录、正式样本和结果文件；
4. 支持从历史记录重新查看任务摘要和计算结果。

验收条件：不连接巡检仪也能查看一次历史校准的全部原始数据和结果。

### 阶段 C：报告和证书输出

优先实现顺序：

1. Excel 校准原始记录；
2. Excel 结果汇总和不确定度表；
3. Word 校准证书；
4. PDF 归档文件。

验收条件：同一归档目录可重复生成内容一致的报告，报告字段全部来源于固化快照和正式样本。

### 阶段 D：多工况校准作业（暂缓，不属于当前范围）

以下内容仅作为未来可选增强保留，不进入当前开发排期：

1. 一个作业包含多个设定温湿度工况；
2. 支持 JJF 1101 的下限、中间点、上限逐工况执行；
3. 支持 JJF 1376 的最低、最高或客户指定温度逐工况执行；
4. 最终结果和报告按作业汇总所有工况。

当前替代方式：每个设定工况新建一个独立任务和作业目录，各自保存布点、正式样本、结果和报告。

### 阶段 E：测试和交付

工作内容：

1. 将临时规则检查迁移为正式测试项目；
2. 增加协议帧、字节序、通道映射、规范规则、公式和 CSV 读写测试；
3. 验证 30～60 min 连续采样、串口拔插、设备无响应和应用异常退出；
4. 清理临时文件和编译警告；
5. 增加运行日志、数据备份、版本号和安装包。

验收条件：真实巡检仪连续运行不被读死，异常退出不丢失已取得样本，软件可在目标电脑独立安装运行。

## 7. 当前执行顺序

1. 已完成普通实时测量独立 CSV 会话，保证未启动正式校准时的设备联调数据也可追溯；
2. 开发单工况 Word 校准证书，所有字段只读取固化 CSV；
3. 由已生成的 Word/Excel 与固化 CSV 生成 PDF 归档，并做页面渲染检查；
4. 将 `tmp/RuleCheck` 迁移为正式测试项目，覆盖规范规则、公式、正式/实时 CSV、Excel 和历史兼容；
5. 增加操作记录、运行日志、备份和异常恢复检查；
6. 完成安装、现场长时间采集和单工况交付验收；多工况不纳入当前执行顺序。

## 8. 当前实施状态（更新至 2026-08-24）

- 阶段 A 已完成代码接入：启动正式校准即建立独立作业目录并固化任务快照；每组正式样本立即追加到测点矩阵和原始通道 CSV；完成时写入结果；停止、采集异常、清空和程序关闭时更新中断状态；写盘失败时禁止把任务标记为完成。
- 阶段 B 已完成首版：历史记录直接扫描 `作业摘要.csv`，支持按关键字、规范和状态筛选，可打开数据根目录、选中作业目录、正式采样 CSV 和校准结果 CSV。
- 文件全部采用 UTF-8 BOM，自动检查已验证 Excel/WPS 友好的中文编码、带逗号字段转义、动态测点列、原始通道明细、结果写入和无数据库历史扫描。
- 阶段 C 的 Excel 原始记录与不确定度表已完成：已完成作业可从结果页或历史记录生成 `报告\校准原始记录.xlsx`，包含“任务与结果、不确定度分量、正式采样、原始通道、任务快照”五张工作表；生成器只读取作业目录中的固化 CSV，不读取可能已变化的当前系统设置。
- 结果计算现在同时返回结构化不确定度预算；完成作业时新增 `不确定度分量.csv`。最终 U、合成标准不确定度和每个分量使用同一计算对象生成，避免结果文件与明细表重复计算后出现偏差。
- Excel 已通过五张工作表重新导入、关键区域检查、公式错误扫描和逐表视觉复核；长依据文本可换行显示。阶段 C 下一步为 Word 校准证书，再完成 PDF 归档。
- 普通实时测量已形成独立会话归档：工作台可选择自动保存，每组完整巡检仪响应立即写入动态矩阵和逐通道原始 CSV，停止、异常和程序关闭时封口；该目录不含正式样本和校准结果，避免联调数据污染规范作业。
- 自动检查已验证实时记录的 UTF-8 BOM、动态测点列、缺失通道 `Missing` 证据、会话计数/终态，以及与正式作业目录和文件名完全隔离。
- 已确认当前产品采用单工况作业模型；规范没有要求软件在一个作业中管理多个工况，多设定点可分别建立独立任务。多工况功能暂缓，不作为当前完成度缺口。

## 9. 不在当前阶段实施的内容

- 不引入任何数据库；
- 不在归档结构稳定前制作最终证书模板；
- 普通实时测量可以独立留存，但绝不把实时趋势记录混入正式校准样本；
- 不在缺少明确技术指标时自动给出合格/不合格结论；
- 不开发同一作业内的多工况编排与汇总；
- 不一次性重写现有 WPF 页面和采集服务。
