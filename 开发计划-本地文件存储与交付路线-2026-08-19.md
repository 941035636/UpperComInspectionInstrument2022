# 温湿度校准系统后续开发计划

更新日期：2026-08-19
适用项目：UpperComInspectionInstrument2022
规范依据：JJF 1101-2019、JJF 1376-2012

## 1. 当前阶段判断

当前软件已具备任务配置、巡检仪连接、实时测量、稳定条件确认、正式采样和规范结果计算能力，已经从 UI 原型进入单工况功能验证阶段。

当前最大的产品缺口不是采集页面，而是正式校准数据在程序退出后无法恢复、历史记录没有真实数据、报告不能生成。因此后续开发必须优先完成“任务—工况—正式样本—结果—报告”的本地归档闭环。

## 2. 已确认的存储原则

本项目不使用 SQLite、SQL Server 或其他数据库。

所有业务数据保存为用户可直接查看、复制和备份的本地文件：

- 表格数据采用带 UTF-8 BOM 的 CSV，Excel、WPS 和 LibreOffice 可直接打开中文内容；
- 每次正式校准使用独立目录，避免不同任务的数据相互覆盖；
- 文件写入采用临时文件替换或逐行追加，尽量降低断电和异常退出造成的文件损坏；
- 任务保存时固化规范、标准器、证书、修正值、现场环境和布点快照；
- 历史记录直接扫描本地作业目录，不建立隐藏数据库索引；
- 报告可根据归档 CSV 重新生成，不依赖巡检仪在线；
- 原始数据文件不得因系统设置后续变化而被重写。

## 3. 本地作业目录结构

默认数据根目录建议使用：

`文档\温湿度校准数据`

每次正式校准建立一个目录：

```text
温湿度校准数据
└─ 20260819-143500_设备名称_作业编号
   ├─ 作业摘要.csv
   ├─ 任务信息.csv
   ├─ 正式采样.csv
   ├─ 校准结果.csv
   ├─ 操作记录.csv
   └─ 报告
      ├─ 校准原始记录.xlsx
      ├─ 校准证书.docx
      └─ 校准归档.pdf
```

第一阶段先实现前四个 CSV 文件；报告目录和办公文档在报告阶段接入。

## 4. 文件职责

### 4.1 作业摘要.csv

用于历史记录快速扫描，一行代表一个校准作业，至少包含：

- 数据格式版本；
- 作业编号；
- 作业状态：采样中、已中断、已完成、保存异常；
- 开始时间、完成时间；
- 规范、校准类型；
- 被校设备名称、型号、出厂编号、委托单位；
- 当前设定温度和湿度；
- 温度、湿度测点数；
- 计划样本数和已保存样本数。

### 4.2 任务信息.csv

采用“字段、值”两列方式保存任务快照，包括：

- 规范和校准点方案；
- 容积分类、工作区/测温区尺寸；
- 布点方式、中心点和布点说明；
- 设定温湿度、传感器类型、被校设备分辨力；
- 环境温度、湿度、气压和负载状态；
- 标准器名称、证书编号、有效期、型号和出厂编号；
- 标准器不确定度、覆盖因子、稳定性变化和通道修正值；
- 采样间隔、样本数、稳定依据和偏离说明。

### 4.3 正式采样.csv

一行代表一组规范正式样本，列结构为：

```text
样本序号,采样时间,被校设备温度示值,被校设备湿度示值,温度1...温度N,湿度1...湿度M
```

该文件只保存正式校准样本，不混入实时趋势刷新数据。

### 4.4 校准结果.csv

保存结果项目、数值、单位和说明。根据规范分别输出：

- JJF 1101：温湿度偏差、均匀度、波动度和扩展不确定度；
- JJF 1376：炉温均匀度、稳定度、偏差、炉内最大温差和扩展不确定度。

## 5. 领域结构演进

当前 `CalibrationTaskContext` 表示一个当前工况，后续逐步演进为：

```text
CalibrationJob 校准作业
├─ 设备、委托、规范和标准器快照
├─ CalibrationCondition 校准工况 1
│  ├─ 设定温湿度和布点
│  ├─ 正式样本
│  └─ 计算结果
├─ CalibrationCondition 校准工况 2
└─ 报告文件
```

迁移过程中保留现有页面和采集逻辑，通过文件存储服务逐步替换静态内存上下文，不进行一次性大重构。

## 6. 开发阶段与验收条件

### 阶段 A：建立可恢复的单工况文件归档

工作内容：

1. 新增本地文件存储服务；
2. 正式校准启动时创建作业目录和任务快照；
3. 每取得一组正式样本立即追加到 `正式采样.csv`；
4. 正式采样完成后写入 `校准结果.csv` 并更新作业状态；
5. 主动停止、关闭软件或写入失败时记录为已中断或保存异常。

验收条件：程序退出后，已经取得的正式样本仍可在 Excel/WPS 中打开；历史记录能够识别已完成和已中断任务。

### 阶段 B：接通历史记录

工作内容：

1. 扫描数据根目录下的 `作业摘要.csv`；
2. 支持按设备、编号、委托单位、规范、日期和状态过滤；
3. 支持打开作业目录、正式样本和结果文件；
4. 支持从历史记录重新查看任务摘要和计算结果。

验收条件：不连接巡检仪也能查看一次历史校准的全部原始数据和结果。

### 阶段 C：报告和证书输出

优先实现顺序：

1. Excel 校准原始记录；
2. Excel 结果汇总和不确定度表；
3. Word 校准证书；
4. PDF 归档文件。

验收条件：同一归档目录可重复生成内容一致的报告，报告字段全部来源于固化快照和正式样本。

### 阶段 D：多工况校准作业

工作内容：

1. 一个作业包含多个设定温湿度工况；
2. 支持 JJF 1101 的下限、中间点、上限逐工况执行；
3. 支持 JJF 1376 的最低、最高或客户指定温度逐工况执行；
4. 最终结果和报告按作业汇总所有工况。

验收条件：多个工况共享同一设备、委托和标准器快照，但各自保留独立布点、正式样本和结果。

### 阶段 E：测试和交付

工作内容：

1. 将临时规则检查迁移为正式测试项目；
2. 增加协议帧、字节序、通道映射、规范规则、公式和 CSV 读写测试；
3. 验证 30～60 min 连续采样、串口拔插、设备无响应和应用异常退出；
4. 清理临时文件和编译警告；
5. 增加运行日志、数据备份、版本号和安装包。

验收条件：真实巡检仪连续运行不被读死，异常退出不丢失已取得样本，软件可在目标电脑独立安装运行。

## 7. 当前执行顺序

1. 提交并推送箱式炉自定义布点、规范图查看和本计划文档；
2. 清理未使用的 `Communication/test.cs`；
3. 实现 CSV 作业目录和文件存储服务；
4. 接入正式采样、完成、中断和保存异常状态；
5. 让历史记录读取真实作业目录；
6. 完成构建、规则和 CSV 存储测试；
7. 更新开发文档并推送新的开发检查点；
8. 在数据底座稳定后开发报告模板和多工况作业。

## 8. 不在当前阶段实施的内容

- 不引入任何数据库；
- 不在归档结构稳定前制作最终证书模板；
- 不把实时趋势缓存当成正式原始记录；
- 不在缺少明确技术指标时自动给出合格/不合格结论；
- 不一次性重写现有 WPF 页面和采集服务。
