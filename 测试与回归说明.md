# 测试与回归说明

## 当前正式入口

在仓库根目录打开 PowerShell，执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\Invoke-Regression.ps1 -Configuration Release
```

脚本固定按以下顺序串行执行，避免多个检查同时构建主工程并争用 `obj` 文件：

1. Release 解决方案构建；
2. 规范规则、协议解析、结果公式、本地归档、Excel/Word/PDF、采集生命周期、日志与异常恢复检查；
3. WPF 任务配置、控件可见性、规范联动和结果展示检查。

每次执行都会在 `TestResults\yyyyMMdd-HHmmss` 生成：

- `regression-results.csv`：步骤、状态、时间、耗时、退出码和输出文件；
- `01-build.log`；
- `02-rule-check.log`；
- `03-ui-flow-check.log`。

`regression-results.csv` 使用带 UTF-8 BOM 的 CSV，可由 Excel/WPS 直接打开。为兼容客户电脑自带的 Windows PowerShell 5.1，脚本内部字段名采用英文；`TestResults` 是本机测试证据目录，不提交 Git，正式发布或客户验收时应把对应批次目录复制到交付测试记录中。

## 当前覆盖范围

- JJF 1101-2019、JJF 1376-2012 的容积、布点、中心点、校准点和采样计划联动；
- 温度 IEEE 754 大端解析、湿度寄存器字节序、异常值及通道角色；
- 温湿度与箱式电阻炉结果公式、不确定度分量和边界场景；
- 正式校准 CSV、普通实时 CSV、历史格式兼容、Excel/Word/PDF 生成；
- 单采集循环、停止等待、快速重启互斥、连续失败退避与请求上限；
- 运行日志、操作记录 CSV、正式作业及实时会话异常退出恢复；
- 任务配置页和工作台的主要 WPF 联动。

## 尚不能由本机自动检查替代的项目

- 真实巡检仪拔插、设备断电、错误从站地址和 8 h 连续通信；
- 目标客户电脑的 USB 转串口驱动、普通用户目录权限和安装/卸载；
- Word 在客户办公软件中的逐页显示与打印；
- PDF 在客户默认阅读器和打印机上的版式；
- 业务人员对两份规范场景测试记录的签字确认。

上述项目必须使用真实硬件和目标电脑执行，不能因为自动回归通过就标记为已完成。
