# 温湿度校准系统正式交付开发计划

制定日期：2026-08-28
适用项目：UpperComInspectionInstrument2022
目标版本：V1.0 正式交付版
规范依据：JJF 1101-2019、JJF 1376-2012

## 1. 交付目标

本计划不再以“页面和功能基本存在”为完成标准，而以客户能够在目标电脑完成安装、连接巡检仪、执行一次单工况校准、保存全部证据、生成报告并在异常后恢复为完成标准。

V1.0 必须形成以下完整闭环：

```text
安装与初始化
→ 维护实验室和标准器资料
→ 配置一个单工况校准任务
→ 连接巡检仪并观察实时数据
→ 按规范确认稳定条件
→ 完成正式采样
→ 计算结果和不确定度
→ 固化本地 CSV
→ 生成 Excel、Word、PDF
→ 历史查询、复核和备份
```

## 2. 已确认的产品边界

- 当前版本只支持“一次任务对应一个工况”，多设定点分别建立独立任务；多工况编排不纳入 V1.0。
- 只覆盖 JJF 1101-2019 环境试验设备温湿度参数校准和 JJF 1376-2012 箱式电阻炉校准。
- 不使用 SQLite、SQL Server 或其他数据库。业务数据继续使用可由 Excel/WPS 直接打开的 UTF-8 BOM CSV，并生成普通 `.xlsx`、`.docx` 和 `.pdf` 文件。
- 报告只能读取已固化的作业 CSV，不得读取可能已经变化的当前内存或系统设置。
- 在没有客户技术要求、设备允差或明确判定依据时，系统不得自动给出“合格/不合格”结论。
- 被校设备和委托信息保持可选；缺失时报告明确显示“未填写（可选）”，不得伪造内容。
- Qt 工程继续只作为功能参考，不进行 Qt 到 WPF 的源码翻译或重构。

## 3. 正式交付完成定义

只有同时满足以下条件，项目完成度才能按 100% 计算：

1. 在约定的 Windows 目标电脑上能够安装、卸载和重新安装，普通用户无需安装 Visual Studio。
2. JJF 1101 和 JJF 1376 的所有约定单工况路径都能从任务配置执行到历史归档。
3. 串口占用、设备无响应、CRC 错误、通道缺失、拔插、程序异常退出和磁盘写入失败都有明确提示，不会把失败任务标记为完成。
4. 已取得的实时记录和正式样本在停止、断线或异常退出后仍可直接用 Excel/WPS 查看。
5. 结果数值、不确定度分量和报告内容均能追溯到同一份固化任务快照和正式样本。
6. Excel、Word 和 PDF 在目标办公软件中版式可读、打印正常，重新生成时业务内容一致。
7. 自动测试、真实设备测试、安装测试和客户验收问题均达到本计划规定的发布门槛。
8. 交付包包含安装程序、版本说明、用户手册、数据字典、测试报告和回退包。

## 4. 当前基线与主要缺口

| 领域 | 当前基线 | 正式交付缺口 |
|---|---|---|
| 主业务流程 | 已完成任务配置、校准工作台、结果、历史和设置 | 仍需最终人工全流程验收和边界收口 |
| 设备通信 | 已实现 Modbus RTU、通道解析、修正、动态矩阵和实时趋势 | 缺少长时间、拔插、断线恢复、端口竞争和设备防读死验收 |
| 规范规则 | 两份规范的容积、布点、中心点、采样计划和现场偏离已联动 | 需要建立正式规范场景测试矩阵并由业务人员签字确认 |
| 计算 | 结果指标和不确定度预算已实现并有确定性检查 | 临时检查需要迁移为正式测试工程并保留基准数据 |
| 本地归档 | 正式校准和普通实时测量 CSV 已独立落盘，历史记录可扫描 | 缺少操作记录、运行日志、备份、磁盘异常和损坏文件恢复策略 |
| Excel | 五张工作表已生成并完成结构检查 | 需要纳入正式回归和目标电脑办公软件验收 |
| Word | 已实现从固化 CSV 生成并通过 OpenXML 结构校验 | 尚未完成逐页版式、分页、打印和签发字段验收 |
| PDF | 已实现从冻结 CSV 生成、历史打开、结构校验和逐页渲染检查 | 仍需在目标客户电脑完成阅读器打开和打印验收 |
| 测试 | `tmp/RuleCheck` 和 `tmp/UiFlowCheck` 已覆盖核心规则 | 仍是临时程序，缺少正式测试框架、测试报告和持续回归入口 |
| 发布 | 当前只能从 Visual Studio/Debug 运行 | 缺少 Release 配置、版本号、安装包、升级与卸载验证 |
| 版本管理 | 已建立并推送 V1.0 单工况交付基线 | 后续各阶段继续按可回退提交推进 |

## 5. 缺陷等级和发布门槛

### P0：禁止交付

- 软件无法启动、无法安装或无法卸载。
- 巡检仪存在并发请求、请求节拍失控或可能被持续错误读取导致死机。
- 正式样本、结果或报告与任务测点映射错误。
- 计算公式、符号、单位或不确定度合成错误。
- 异常退出后已取得样本丢失，或者失败任务被标记为“已完成”。
- 报告读取当前设置而不是固化归档，导致历史报告内容漂移。

### P1：正式版必须关闭或取得书面豁免

- 页面关键按钮状态与实际流程不一致。
- 错误提示无法指出端口、请求、响应或归档目录。
- Word/PDF 出现截断、空白页、表格越界或关键字段缺失。
- 历史记录无法打开完整原始数据和报告。
- 安装后缺少运行依赖、串口驱动说明或数据目录权限异常。

### P2：允许登记到后续版本

- 不影响校准正确性的视觉细节和操作效率问题。
- 非当前范围的多工况汇总、模板设计器和高级统计功能。

发布门槛：P0 必须为 0；P1 必须为 0，或由项目负责人和客户形成书面豁免；所有自动检查通过；现场验收记录完整。

## 6. 分阶段开发计划

### 阶段 0：建立可回退的交付基线

预计工作量：1～2 人日。

工作内容：

1. 盘点当前 38 个已修改文件和 5 个未跟踪项，区分业务源码、测试资产、规范图片、对话备份和临时构建文件。
2. 将已验证的业务修改形成 Git 提交并推送远端；提交前不得删除用户现有改动。
3. 为 `tmp`、`verify-build`、运行日志和测试输出制定保留或忽略规则，避免临时文件进入正式发布包。
4. 固定 V1.0 数据格式版本、默认数据根目录、目标架构和最低 Windows 版本。
5. 建立 Release 构建命令和版本号规则，例如 `1.0.0`。

验收条件：

- 工作树中的正式源码和测试资产可追溯；远端存在可回退提交。
- Release 独立目录构建为 0 错误、0 警告。
- 当前功能异常时能够恢复到交付基线。

### 阶段 1：收口结果与报告流程

预计工作量：4～6 人日。

工作内容：

1. 删除或整合仍显示“报告未接入”的旧 `ReportView`，报告入口统一放在“结果与报告”和“历史记录”。
2. 对 Word 证书进行逐页视觉检查，修正分页、标题、表格宽度、签字区、页眉页脚和可选字段显示。
3. 明确 Word 证书状态为“待审核签发”，签发前不得伪装成正式已批准证书。
4. 实现 `校准归档.pdf`，内容只来源于固化 CSV 和已经生成的报告；生成后进行页数、关键文本和逐页渲染检查。
5. 在结果页和历史页统一显示 Excel、Word、PDF 的“未生成、已生成、生成失败”状态，并提供打开文件和打开目录入口。
6. 更新旧开发计划中“Word 尚未实现”等已经过期的描述。

验收条件：

- 已完成作业可在断开巡检仪后重复生成 Excel、Word 和 PDF。
- 三类报告的任务编号、设备信息、规范、设定值、结果和不确定度与 CSV 一致。
- Word 和 PDF 每一页均无截断、越界、异常空白页和不可读字体。
- 1.0 历史归档缺少分量时明确提示限制；1.1 归档缺文件时阻止生成报告。

### 阶段 2：通信可靠性和异常恢复

预计工作量：5～8 人日。

工作内容：

1. 确认全程序只有一个串口所有者和一个 Modbus 请求队列，页面切换、实时测量和正式采样不得并发读同一设备。
2. 固化巡检仪完整通道块读取策略、请求间隔、超时、清缓冲、CRC 校验和失败退避，避免高频重试把设备读死。
3. 增加端口被占用、设备无响应、短帧、异常码、CRC 错误、通道特殊值和拔插后的明确状态机。
4. 支持安全停止与重新连接；旧请求取消完成前不得开始新请求。
5. 增加滚动运行日志，至少记录版本、启动关闭、串口参数、连接状态、请求摘要、异常类型、作业编号、写盘错误和报告生成结果；日志不得保存无必要的个人信息。
6. 增加 `操作记录.csv`，记录任务保存、开始实时测量、启动正式采样、停止、中断、完成、生成报告等业务动作。
7. 程序启动时扫描“采样中”作业并明确标记为上次异常中断，禁止静默继续或覆盖。
8. 验证磁盘满、目录只读、文件被 Excel 占用和数据根目录不可访问时的处理。

验收条件：

- 断线、拔插和超时不会造成界面假死、重复请求或端口无法释放。
- 任何写盘失败都不会把任务标记为完成。
- 异常退出后已追加的 CSV 行仍可读取，重启后能够识别中断作业。
- 请求频率和重试次数有上限，真实巡检仪不会因软件轮询进入死机状态。

### 阶段 3：建立正式自动化测试体系

预计工作量：5～8 人日。

工作内容：

1. 将 `tmp/RuleCheck` 迁移为正式单元/集成测试项目，保留现有确定性样本和断言。
2. 将 `tmp/UiFlowCheck` 迁移为正式 WPF 流程测试项目，避免测试直接污染用户配置和数据目录。
3. 建立以下测试组：协议帧与 CRC、字节序、温度 50 通道、湿度 10 探头、通道角色、修正值、规范联动、公式、不确定度、CSV 转义、历史兼容、Excel、Word、PDF。
4. 为每项结果公式保存人工核算基准样本，测试必须比较具体数值、符号和单位，而不是只检查“有结果”。
5. 增加异常样本：通道缺失、`-9999.9` 特殊值、NaN、短帧、超时、重复通道、错误中心点、文件缺失和损坏 CSV。
6. 建立一键回归命令，生成可归档的测试结果摘要。

验收条件：

- Release 构建和全部自动测试可由一条标准命令执行。
- 核心计算、存储和报告生成不存在依赖真实串口的随机测试。
- 每个已修复 P0/P1 缺陷都有回归用例。

### 阶段 4：真实巡检仪和规范场景验收

预计工作量：5～10 人日，需要巡检仪、被校设备和客户/业务人员配合。

测试矩阵：

1. JJF 1101 温度任务：小于等于 2 m³、大于 2 m³、规范点数和实际工作位置调整。
2. JJF 1101 温湿度任务：小于等于 2 m³、大于 2 m³、湿度 O 点不同接线映射。
3. JJF 1376：小于等于 0.15 m³ 的 5 点方案、大于 0.15 m³ 的 9 点方案、图 1、图 2 和有依据的现场自定义。
4. 每个场景分别完成实时观察、稳定确认、正式采样、结果、归档和报告，不在同一任务中组织多个工况。
5. 连续运行测试：至少完成规范 30～60 min 完整周期，并追加 8 h 通信稳定性测试。
6. 故障注入：串口被占用、采集中拔线、设备断电、无响应、错误从站地址、应用关闭、强制结束进程和文件被占用。
7. 数据核对：巡检仪屏幕、软件当前值、CSV 原始值、修正后值、结果和报告逐级抽样核对。

验收条件：

- 客户或业务人员确认布点、稳定条件、采样计划和结果口径与规范一致。
- 8 h 测试无设备读死、无限重试、内存持续增长、界面冻结或串口无法释放。
- 所有故障均留下可理解的状态、日志和可恢复数据。

### 阶段 5：安装、升级和用户文档

预计工作量：3～5 人日。

工作内容：

1. 生成 `Release / win-x64` 自包含发布目录，首版不依赖客户电脑预装 .NET SDK，也不优先采用难排错的单文件打包。
2. 制作安装程序，支持安装目录选择、开始菜单快捷方式、卸载和版本显示；业务数据始终保存在用户文档目录，卸载不得删除业务数据。
3. 明确 USB 转串口驱动、串口参数、管理员权限和防火墙要求；如无需管理员权限，应完成普通用户验证。
4. 验证覆盖安装、升级安装、卸载重装和旧数据继续可读。
5. 编写用户手册、快速开始、常见故障、数据目录说明、CSV 字段字典、备份恢复说明和报告审核说明。
6. 提供默认配置示例，但不得把示例标准器证书或修正值当作客户真实数据。

验收条件：

- 全新目标电脑能够仅使用交付包完成安装和首个任务。
- 卸载软件不删除历史 CSV 和报告。
- 旧版本作业可以被新版本历史页读取，不能兼容时必须明确提示。

### 阶段 6：客户验收与版本冻结

预计工作量：3～5 人日。

工作内容：

1. 使用客户真实设备执行约定的验收用例，并保存验收作业目录和测试记录。
2. 只修复验收发现的 P0/P1；版本冻结后不再加入未评审的新功能。
3. 生成版本说明、已知限制、安装包校验值和回退包。
4. 对最终源码、测试结果、安装包和文档打版本标签并推送远端。
5. 由项目负责人、开发人员和客户代表确认交付清单。

验收条件：

- P0 为 0，P1 为 0 或已书面豁免。
- 安装测试、自动测试、真实设备测试和报告复核全部有记录。
- 最终安装包能够从版本标签对应源码重复构建。

## 7. 建议工期与关键路径

在一名主开发人员、客户能够提供真实硬件并及时确认业务问题的条件下，预计还需 26～44 人日，建议安排 5～8 个自然周。

| 周期 | 重点工作 | 可交付结果 |
|---|---|---|
| 第 1 周 | 阶段 0、报告流程收口 | 可回退基线，Word 定版，PDF 首版 |
| 第 2～3 周 | 通信可靠性、日志和异常恢复 | 可进行故障测试的候选版本 |
| 第 3～4 周 | 正式测试工程和报告回归 | 一键自动回归与测试摘要 |
| 第 4～6 周 | 真实设备与规范场景验收 | 现场测试记录和问题清单 |
| 第 6～7 周 | 安装包、升级和用户文档 | Release Candidate |
| 第 7～8 周 | 客户验收、缺陷关闭、版本冻结 | V1.0 正式交付包 |

关键路径是“通信可靠性 → 真实设备长时间测试 → 客户验收”。如果硬件、被校设备或客户场地不能及时提供，代码可以继续开发，但正式交付日期不能被视为已确认。

## 8. 下一轮立即执行的任务

按优先级执行，不并行扩展新功能：

1. 在可正常捕获/打印 Word 的目标电脑完成证书逐页视觉和打印验收。
2. 建立正式测试解决方案并迁移现有 103 项左右的规则/UI断言。
3. 增加通信请求串行化检查、运行日志和 `操作记录.csv`。
4. 准备真实硬件故障与长时间测试脚本。
5. 建立 Release 自包含发布和安装包骨架。

## 9. 最终交付清单

- V1.0 安装程序和自包含发布包；
- 对应源码版本标签和可回退提交；
- 用户手册、快速开始和常见故障说明；
- 数据目录说明和 CSV 字段字典；
- JJF 1101、JJF 1376 场景测试记录；
- 协议、公式、存储和报告自动测试结果；
- 真实巡检仪长时间与故障恢复测试报告；
- Excel、Word、PDF 样例作业；
- 版本说明、已知限制、校验值和回退包；
- 客户验收确认记录。

## 10. 计划维护规则

- 每完成一个阶段，在 `开发文档.md` 记录代码、数据格式、界面和测试变化。
- 本文件作为 V1.0 正式交付主计划；旧的开发计划继续保留历史决策，但状态冲突时以本文件和当前代码为准。
- 任何新增需求必须先判断是否影响规范、数据格式、报告模板、工期和既有验收用例，再决定进入 V1.0 还是后续版本。

## 11. 实施记录

### 2026-08-28：阶段 0 首轮基线整理

- 已完成正式源码、可复现检查和本地过程产物分类，并建立对应忽略规则。
- 已固定 V1.0.0 程序版本，Release 构建和两组串行回归检查通过。
- 已形成提交 `7bacfb0 feat: 建立V1.0单工况交付基线` 并推送远端 `master`，阶段 0 已关闭。

### 2026-08-28：阶段 1 报告流程收口（进行中）

- 已删除旧 `ReportView`，报告入口统一到结果页和历史页。
- 已完成 PDF 归档服务、结果/历史入口、三类报告状态、1.0/1.1 归档兼容规则和自动检查。
- PDF 已逐页渲染检查，中文换行、表格、分页、签字区和页眉页脚正常。
- Word 结构校验通过，但受当前电脑缺少文档渲染器和 Windows 图形捕获接口异常影响，逐页视觉与打印验收尚未关闭；阶段 1 因此保持“进行中”。

### 2026-09-03：阶段 2 通信可靠性与异常恢复（代码首轮完成，待硬件验收）

- 全程序继续保持一个 `ModbusRtuClient`、一个协议读取器和一个采集循环；旧循环未完全退出前拒绝重新启动，界面暂停会异步等待当前串口读结束。
- 通信失败采用 1～5 s 有界退避，连续 3 次失败后停止请求并释放串口；一次完整读取成功即清零失败计数，偶发超时不会立即中断作业。
- 已增加按日运行日志和全局 `操作记录.csv`，均为 UTF-8 BOM CSV；覆盖连接、采集、正式校准、异常和报告生成关键动作。
- 已增加启动恢复：上次异常退出遗留的正式作业和实时会话自动标记“已中断”，已经落盘的数据保持不变，并向用户显示恢复结果。
- 模拟读取器回归已验证快速重启互斥、失败退避上限和停止后可重新启动；临时目录回归已验证日志编码、正式作业恢复和实时会话恢复。
- 本阶段代码项仍缺真实巡检仪拔插、端口竞争、设备断电、8 h 连续运行及磁盘满/只读/文件占用实测，因此阶段 2 尚不能关闭。

### 2026-09-03：阶段 3 自动回归体系（首轮入口完成，继续迁移）

- 两个现有检查项目已加入主解决方案，并提供 `scripts\Invoke-Regression.ps1` 统一串行入口；Release 构建、规则/集成检查和 WPF 联动检查不再需要人工逐条输入命令。
- 每次回归生成独立日志及带 UTF-8 BOM 的 `regression-results.csv`，可直接归档和在 Excel/WPS 中查看；本机首次完整执行三步均通过。
- 已新增测试说明并区分“自动通过”和“必须真实硬件/目标电脑验收”，避免把模拟通信测试误报为现场验收。
- 当前 `RuleCheck`、`UiFlowCheck` 仍是大文件控制台断言程序；后续需按协议、规范、公式、存储、报告、通信生命周期和 UI 联动拆分为正式测试用例，并输出可统计的用例数量和失败明细，因此阶段 3 尚未关闭。

### 2026-09-03：阶段 5 自包含发布（便携候选完成，安装包未完成）

- 已提供默认先跑完整回归的 `scripts\Publish-Release.ps1`，生成 `Release / win-x64` 自包含多文件目录、ZIP 和逐文件 SHA-256 清单；目标电脑无需安装 .NET SDK。
- 已加入快速开始、V1.0 候选版本说明和发布构建说明；业务数据继续位于用户文档目录，不随发布目录覆盖而删除。
- 自包含目录和 ZIP 已成功生成，程序启动/正常关闭冒烟通过。
- 本机缺少安装包编译器，EXE 也未代码签名；覆盖安装、快捷方式、卸载重装、旧数据兼容和全新目标电脑仍未验收，因此阶段 5 仅完成便携发布候选，不能关闭。
