# 工业设备校准系统 WPF 开发文档

## 1. 文档目的

本文档用于记录本项目的总体目标、架构决策、规范依据、功能开发过程和重要变更，便于后续维护、测试和功能扩展。

每完成一个重要功能，必须同步补充以下内容：

- 修改日期
- 功能目标
- 涉及文件
- 实现结果
- 验证结果
- 已知限制和下一步计划

## 2. 项目定位

本项目是基于 WPF 和 .NET 8 开发的工业设备校准上位机软件，主要用于连接温湿度巡检仪或相关标准器，完成设备数据采集、校准计算、结果判定、记录保存和报告生成。

WPF 工程和旧 Qt 工程是两个独立项目：

- Qt 工程只作为既有功能、业务流程、通信协议和计算结果的参考。
- WPF 工程不复制 Qt 工程架构，也不直接翻译 Qt 源代码。
- Qt 中已有的成熟功能，WPF 需要逐项补齐。
- Qt 中缺失但规范要求或工业软件需要的功能，WPF 需要独立完善。

旧 Qt 工程位置：

`F:\WorkSpace\Resources\无线巡检仪源码\无线巡检仪源码`

## 3. 规范依据

当前软件主要依据以下规范开发：

- JJF 1101-2019《环境试验设备温度湿度参数校准规范》
- JJF 1376-2012《箱式电阻炉校准规范》

### 3.1 JJF 1101-2019

主要校准项目：

- 温度偏差
- 温度均匀度
- 温度波动度
- 相对湿度偏差
- 相对湿度均匀度
- 相对湿度波动度

典型采样规则：

- 设备稳定后开始采样
- 一般等待 30 分钟
- 每 2 分钟采集一次
- 30 分钟内记录 16 组数据
- 容积不大于 2 m³ 时，温度 9 点、湿度 3 点
- 容积大于 2 m³ 时，温度 15 点、湿度 4 点

### 3.2 JJF 1376-2012

主要校准项目：

- 炉温均匀度
- 炉温稳定度
- 炉温偏差
- 炉内最大温差

典型采样规则：

- 通常为空载校准
- 炉温达到设定值并稳定后开始采样
- 连续记录 60 分钟
- 每 3 分钟记录一次
- 至少记录 20 组数据
- 容积不大于 0.15 m³ 时，5 个测点
- 容积大于 0.15 m³ 时，9 个测点

## 4. 当前目标架构

WPF 软件按任务工作流组织，而不是把所有功能堆在一个窗口中：

```text
校准任务
  ├─ 规范与设备选择
  ├─ 测点和采样计划配置
  ├─ 标准器及修正值配置
  ├─ 稳定等待与数据采集
  ├─ 规范计算
  ├─ 结果判定
  ├─ 原始记录
  └─ 报告与证书归档
```

代码层次目标：

```text
Views           WPF 页面和交互
ViewModels      页面状态和命令
Services        采集服务、任务服务、计算服务
Communication   串口、Modbus RTU、数据帧解析
Models          任务、测点、采样快照、结果模型
Persistence     配置、历史数据和报告数据
Reports         Excel、Word、PDF 输出
```

## 5. 当前页面工作流

当前页面流转如下：

```text
TaskView
  → SchemeView
  → CalibrationView
  → ResultView
  → ReportView
```

页面职责：

### TaskView

校准任务工作台，负责展示当前任务和进入各个阶段。

### SchemeView

配置规范、设备类型、测点数量、采样间隔、目标次数、稳定等待时间和标准器修正值。

### CalibrationView

展示通信状态、稳定状态、采样进度和测点实时数据，并提供采集控制入口。

### ResultView

展示温度偏差、均匀度、波动度/稳定度、不确定度和最终判定。

### ReportView

配置原始记录、结果汇总、校准证书和不确定度报告输出。

## 6. 已完成开发记录

### 2026-08-17：建立任务工作流界面

功能目标：

将原有“首页 + 多个占位按钮”调整为以校准任务为核心的工作流界面。

主要修改：

- 完成校准任务工作台页面。
- 完成校准方案配置页面。
- 完成采集与校准执行页面骨架。
- 完成校准结果页面骨架。
- 完成记录与报告归档页面骨架。
- 将主窗口中的占位弹窗改为实际页面导航。
- 增加 JJF 1101 和 JJF 1376 的采样规则提示。

涉及文件：

- `MainWindow.xaml.cs`
- `Views/TaskView.xaml`
- `Views/TaskView.xaml.cs`
- `Views/SchemeView.xaml`
- `Views/SchemeView.xaml.cs`
- `Views/CalibrationView.xaml`
- `Views/CalibrationView.xaml.cs`
- `Views/ResultView.xaml`
- `Views/ResultView.xaml.cs`
- `Views/ReportView.xaml`
- `Views/ReportView.xaml.cs`

验证结果：

- `dotnet build UpperComInspectionInstrument2022.sln --no-restore`
- 编译成功
- 0 个错误
- 保留原有可空引用警告，尚未在本次界面改造中处理

已知限制：

- 当前方案配置尚未形成真正的任务对象。
- 采集执行页面的按钮暂时只更新页面状态，尚未绑定真实采集服务。
- 结果页面暂时没有接入规范计算服务。
- 报告页面暂时没有接入 Excel、Word 或 PDF 生成服务。
- 页面之间的任务状态还没有统一共享。

## 7. 当前代码中的重要问题

### 通信层

- `ModbusRtuClient` 已实现功能码 03 的基础读取，但异常响应解析仍需完善。
- 串口打开、关闭和采集任务生命周期需要统一管理。
- 采集服务停止时需要等待后台循环真正结束。

### 数据层

- `MeasurementSnapshot` 当前主要表示采集快照，还不是完整的校准任务数据模型。
- 需要区分通信有效、测量有效、计算有效和校准合格。
- 所有原始数据必须可追溯，不能只保存最终计算值。

### 业务层

- 需要建立 `CalibrationTask`、`CalibrationPoint`、`CalibrationSample`、`CalibrationResult` 等模型。
- JJF 1101 和 JJF 1376 需要使用不同的采样计划和计算器。
- 规范、设备类型、测点方案和采样策略不能通过界面文字判断。

### 2026-08-17：完善实时测量页面并接入采集服务

功能目标：

让实时测量页面能够配置通信参数、打开实际串口、启动自动采集并显示巡检仪返回的通道数据。

主要修改：

- 增加串口选择和刷新功能。
- 增加波特率配置，默认 115200。
- 增加 Modbus 从站地址配置，默认 1，限制为 1～247。
- 增加采样周期配置，默认 2000 ms，最小 200 ms。
- 接入 `ModbusRtuClient.Open`。
- 接入 `InspectionDataAcquisitionService.Start` 和 `Stop`。
- 将数据有效性统计改为使用 `InspectionChannelData.IsValid`。
- 增加通道类型、原始 HEX 数据和测量状态显示。
- 增加设备连接状态、采集状态和采样进度反馈。
- 页面离开时停止采集并解除事件订阅，避免重复回调。

涉及文件：

- `Views/RealTimeMeasurementPage.xaml`
- `Views/RealTimeMeasurementPage.xaml.cs`
- `MainWindow.xaml.cs`

验证结果：

- 解决方案编译成功。
- 0 个编译错误。
- 本机当前检测到 `COM1`、`COM7`。
- 尚未实际确认哪个串口连接的是巡检仪，因此未自动发起现场采集。

现场验证步骤：

1. 启动 WPF 软件，进入“实时测量”。
2. 点击“刷新串口”。
3. 选择巡检仪对应的串口。
4. 确认波特率为 115200、从站地址为 1。
5. 点击“开始实时采集”。
6. 确认状态变为“设备已连接”和“正在采集”。
7. 检查采集记录、有效/异常通道数、通道值和原始 HEX 数据。
8. 如果发生超时或 CRC 错误，记录串口号、设备地址、原始报文和错误提示。

当前限制：

- 当前巡检仪服务固定读取保持寄存器 `0x0001` 起始的 100 个寄存器，并解析 50 路温度通道。
- 采集周期控制已经接入，但还没有与 JJF 1101/1376 的规范采样计划自动联动。
- 页面中的采集执行尚未形成完整的校准任务对象。
- 真实设备验证需要确认物理串口、从站地址、寄存器映射和厂家字节序。

### 2026-08-17：补齐 Qt 实时测量参数并拆分连接/测量流程

根据 Qt 实时测量界面补充以下参数：

- 校准类型：温度、湿度、温度+湿度、温湿度
- 温度测点数量
- 湿度测点数量
- 校准次数
- 温度传感器类型：pt100、cu50、cu100、K/E/J/T 型
- 校准间隔
- 中心点号
- 中心温度和中心湿度显示位
- 中心点间隔

操作流程调整为：

```text
选择串口和通信参数
  → 连接巡检仪
  → 连接成功后点击实时测量
  → 接收并显示测量数据
```

本次调整中，“连接巡检仪”和“实时测量”已经分成两个按钮。连接按钮只负责打开并保持串口；实时测量按钮负责启动 `InspectionDataAcquisitionService`。

当前限制：

- 这些参数已经进入界面，但温度/湿度点数、校准次数和中心点参数还没有完全传入底层采集服务。
- 当前底层仍固定读取 50 路温度通道，寄存器起始地址为 `0x0001`，读取 100 个保持寄存器。
- 真实设备验证需要在页面选择实际巡检仪串口后进行，当前机器检测到 `COM1` 和 `COM7`，尚未确认设备对应端口。

### 2026-08-17：修复采集超时风险并调整实时测量布局

问题现象：

- 采集时报错“读取超时，没有收到完整的 Modbus 响应”。
- 采集记录区域高度过小。
- 不同校准类型的参数全部显示，容易造成误配置。

原因分析：

Qt 工程针对不同校准类型使用不同的 Modbus 读帧，WPF 原先始终读取 `0x0001` 起始的 100 个寄存器，无法覆盖湿度和温湿度数据协议。

协议分流：

- 温度：起始地址 `0x0001`，数量 `100`，解析 50 路 32 位浮点温度。
- 湿度：起始地址 `0x0065`，数量 `20`，按 16 位数值除以 100 解析温湿度通道。
- 温度+湿度/温湿度：连续读取温度帧和湿度帧。

界面调整：

- 采集记录和当前详细数据区域改为按剩余空间自适应布局。
- 校准类型为温度时禁用湿度点数。
- 校准类型为湿度时禁用温度点数和温度传感器类型。
- 温度+湿度、温湿度类型启用两类参数。
- 保留中心点号、中心温度和中心湿度显示区域。

涉及文件：

- `Services/InSpectionMeterService.cs`
- `Services/InspectionDataAcquisitionService.cs`
- `Views/RealTimeMeasurementPage.xaml`
- `Views/RealTimeMeasurementPage.xaml.cs`

验证结果：

- 解决方案编译成功。
- 0 个错误。
- 真实设备帧验证仍需在正确串口和从站地址下执行。

## 8. 后续开发顺序

1. 建立统一的校准任务数据模型。
2. 将 `SchemeView` 配置保存为当前任务对象。
3. 将任务对象传递到 `CalibrationView`。
4. 接入串口配置和真实采集服务。
5. 实现稳定等待、采样进度和原始数据保存。
6. 实现 JJF 1101 计算器。
7. 实现 JJF 1376 计算器。
8. 实现结果判定和不确定度数据结构。
9. 实现历史任务存储和查询。
10. 实现 Excel、Word、PDF 报告输出。

## 9. 开发约定

- 修改前先确认 Qt 工程中的既有功能和规范要求。
- Qt 工程只读，不在 Qt 工程中提交 WPF 相关修改。
- WPF 页面只负责交互和展示，计算逻辑放在服务或领域类中。
- 不使用控件显示文本作为业务判断条件。
- 重要计算必须有可重复执行的输入、输出和测试数据。
- 每个重要功能完成后更新本文档。
- 修改后至少执行一次解决方案级别编译。

### 2026-08-17：现场采集无响应诊断

- 使用与 Qt 完全一致的 Modbus RTU 请求帧进行脱离 WPF 的只读测试：温度帧为 `01 03 00 01 00 64 15 E1`，湿度帧为 `01 03 00 65 00 14 55 DA`。
- COM1、COM7 在 115200/8N1、从站地址 1 下均返回 0 字节；因此当前“读取超时”已确认不是采集记录区域布局或 WPF 数据解析造成的。
- 串口打开不等于巡检仪已响应。WPF 采集异常提示已补充端口、请求帧和响应帧，便于现场区分端口、波特率、从站地址、设备供电及 USB 转换器问题。
- 现场验证顺序：关闭 Qt 或其他串口工具 → 确认巡检仪供电和 RS232/RS485 接线 → 在设备管理器确认实际 USB 串口号 → 确认设备参数为 Modbus RTU、115200、8N1、从站 1 → 再执行“连接巡检仪”和“实时测量”。

### 2026-08-17：依据巡检仪 Modbus 协议修复串口生命周期

- 《巡检仪modbus通讯协议_50温度10湿度.doc》确认：1～50 路温度寄存器为 `0x0001～0x0064`，10 路湿度/温度对寄存器为 `0x0065～0x0078`；温度数据为 32 位浮点数，湿度数据按 `0.01` 缩放；通讯参数为 115200、8N1。
- 原程序停止采集时只取消任务，没有等待当前串口读取完成；重连或关闭窗口时可能与读取线程并发操作 COM7，导致 `Access to the path 'COM7' is denied` 或设备处于半帧状态。
- `ModbusRtuClient` 现在对打开、关闭、发送和接收使用同一把 I/O 锁，并保证打开失败时释放临时串口对象；采集异常和页面离开时主动关闭串口。
- 温度+湿度模式在两次读取之间增加约 200 ms 间隔，与 Qt 原程序保持一致，降低巡检仪连续请求处理异常的风险。
- 现场复核发现 COM7 仅存在于 `SerialPort.GetPortNames()` 的残留列表中，系统设备枚举和 `mode COM7` 均表明该端口当前不可用；实时测量页面已改为刷新时实际尝试打开串口，只展示当前可打开的端口，避免误选失效端口。

### 2026-08-17：重构实时测量页面展示层级

- 页面按“连接状态 → 通讯与采集模式 → 操作控制 → 本次实时测量摘要 → 当前通道明细 → 采集快照”重新布局，当前通道明细获得主体区域，避免实时数据被快照表挤压。
- 实时摘要显示温度平均值/范围、湿度平均值/范围、选定中心点值、采集更新时间、采集序号及有效/异常通道数。
- 通道明细保留通道号、类型、实时测量值、单位、通信状态、原始数据和寄存器地址；明确“通信有效不等于校准合格”，校准合格判定留在校准结果模块。
- 校准类型切换时同步隐藏无关的温度点数、湿度点数、传感器类型和中心值；中心点选项按当前温度/湿度点数自动更新。
- 设备状态分为“未连接”“串口已打开，等待设备响应”“设备已响应”，收到第一帧有效采集数据后才显示设备已响应。

### 2026-08-17：实时测量与校准执行合并为校准工作台

- 原“实时测量”和“校准执行”被拆成两个页面，用户无法在数据稳定后直接进入正式采样；现将实时测量页升级为“校准工作台”，保留任务管理和结果页面，但在执行阶段使用同一个工作流。
- 工作台布局调整为左侧大面积实时趋势与通道数据，右侧为通讯参数、校准参数和执行控制，底部仅保留全局状态提示。
- 实时采集启动后持续接收数据；最近 5 组温度平均值波动不超过 0.2 ℃、湿度平均值波动不超过 1.0 %RH 时显示“数据稳定，可启动校准”。该状态是工作台预判，最终稳定性与校准合格判定仍由规范计算模块完成。
- 点击“启动校准”不停止实时测量、不重新打开串口、不重新配置参数，而是沿用当前采集流进入校准采样状态；计划次数大于 0 时达到计划组数后提示采样完成。
- 曲线当前绘制最近 60 组温度/湿度平均值趋势，通道明细保留每次最新完整数据，原始寄存器数据继续作为诊断字段保留。

### 2026-08-17：实时数据改为测点矩阵展示

- 删除校准工作台右侧的“采集快照”表，避免把操作人员最关心的测点数据压缩成辅助记录。
- 主数据区改为“采集序号/时间 + 测点列”的矩阵：温度点数为 N 时生成“温度1～温度N”列，湿度点数为 N 时生成“湿度1～湿度N”列；温度+湿度模式同时生成两组列。
- 每次完整采集形成一行，最多保留最近 200 行用于实时观察；切换校准类型或点数时自动重建列结构，采集期间参数锁定，保证表格与采集任务保持一致。
- 主表移除原始 HEX 和寄存器地址等诊断字段，保留在底层数据对象中，后续通过通讯诊断功能查看。

### 2026-08-17：修复实时摘要区域文本重叠

- 摘要区五列改为按信息量分配宽度，避免平均值、稳定性和数据质量等长文本互相覆盖。
- 摘要值支持自动换行，适当降低字号并增加摘要行高度，保证不同窗口宽度下仍可读。

### 2026-08-17：按协议原始帧修正温度通道解析

- 根据巡检仪协议确认温度 CH1 对应保持寄存器 `0x0001`、`0x0002`，每个温度通道占连续两个 16 位寄存器。
- 温度解析改为直接从 Modbus 响应数据区偏移 `3 + 通道序号 * 4` 读取 4 字节，避免经过寄存器数组时发生通道错位。
- 保留原始 HEX、寄存器地址和寄存器值，便于现场核对 CH1 是否收到正确的 `4` 字节浮点数据。

### 2026-08-17：切换页面时保持实时采集

- 实时测量页面改为由主窗口缓存并复用，不再每次进入都创建新页面。
- 页面卸载不再停止采集、关闭串口或取消数据事件订阅；切换到校准任务、结果或历史页面时，后台采集继续运行。
- 返回实时测量页面时继续显示原有参数、测量矩阵、趋势图和最新状态；程序真正关闭时仍由主窗口统一停止采集并释放串口。

### 2026-08-17：按职责拆分校准配置页面

- “校准任务配置”负责规范、设备类型、容积、校准类型、设定值、测点数量、中心点和采样计划。
- “系统设置”负责标准器名称、证书、有效期、型号、出厂编号、温湿度不确定度、覆盖因子和实验室环境条件。
- “校准工作台”保留串口通讯、实时采集、稳定性观察、测点矩阵和启动校准等执行操作；任务配置保存后会同步到工作台。
- 增加 `CalibrationTaskContext` 作为当前任务配置的会话状态，避免在实时工作台重复录入任务参数。
- 任务保存后，实时工作台中的同类参数自动带入并锁定，避免任务配置页与执行页出现两套互相矛盾的参数。

### 2026-08-17：按完整校准流程复核 UI 与操作边界

- 按“任务配置 → 系统设置 → 校准工作台 → 校准结果 → 记录报告”的流程复核页面职责。
- 实时工作台保留大面积趋势图和测点矩阵，右侧只承担通讯、当前任务摘要和执行控制，避免把报告元数据挤入实时数据区。
- 结果页增加“采样未完成”状态；只有计划采样完成后才允许进入记录与证书生成，防止未完成数据被误归档。
- 对 JJF 1101-2019，任务页保留温湿度类型、容积、温湿度测点和校准点；对 JJF 1376-2012，后续应进一步自动隐藏湿度字段并切换箱式电阻炉专用计算结果。
- 本轮完成的是流程和 UI 边界复核，真实设备通讯、规范计算和证书输出仍需在接入实际设备及计算模块后现场验证。

### 2026-08-17：首页定位与菜单顺序整改

- 首页由功能入口拼图调整为“校准任务总览”，显示当前任务状态、下一步提示和继续任务入口。
- 左侧菜单调整为“首页 → 校准任务 → 校准工作台 → 校准结果 → 历史记录 → 系统设置”，符合现场校准的实际操作顺序。
- “实时测量”菜单统一更名为“校准工作台”，明确该页面同时承担实时测量、稳定观察和正式校准采样。

### 2026-08-18：简化校准任务配置界面

- 将任务配置压缩为“选择校准对象 → 配置校准点 → 采样计划”三步结构，减少首屏表单层级。
- 将稳定等待和中心点等低频参数放入“高级参数”，常用操作只保留规范、设备、校准类型、设定值、测点数、采样次数和间隔。
- 校准类型按索引联动温度/湿度字段，温度模式隐藏湿度配置，湿度模式隐藏温度配置，避免无效参数干扰。
- 保存任务后清除上一任务的完成状态，防止新任务错误继承旧结果。

### 2026-08-18：移除重复概览首页

- 复核发现原“概览/首页”仅重复左侧菜单入口，没有提供独立的任务状态、报警或历史摘要价值。
- 当前版本隐藏概览菜单，程序启动后直接进入“校准任务”，减少一次无效点击。
- 左侧菜单作为唯一主导航，顺序为“校准任务 → 校准工作台 → 校准结果 → 历史记录 → 系统设置”。
- 后续只有在首页接入真实的当前任务状态、设备报警、采样进度和最近任务后，才考虑恢复独立概览页。

### 2026-08-18：全工程流程审计与第二轮修复

- 完成主工程结构、页面入口、采集服务、Modbus 客户端、任务配置、结果页和报告页的静态审计；工程当前没有自动化测试项目，因此 `dotnet test` 未执行实际测试用例。
- 修复采集服务的生命周期竞态：停止采集只发出取消信号，由采集循环在读操作收尾后释放取消源，避免串口读线程与停止/重连操作并发释放资源。
- 修复通信层可空性和串口对象生命周期告警，当前 WPF 主工程编译结果为 0 个警告、0 个错误。
- 系统设置页增加真实的本机用户配置持久化，保存标准器名称、证书编号、有效期、型号、出厂编号、校准机构、不确定度、覆盖因子和环境条件；重新启动软件后自动恢复。
- 系统设置页增加数字输入校验，防止不确定度、覆盖因子和环境条件输入非法值后继续保存。
- 旧“校准执行”入口改为兼容跳转到统一“校准工作台”，避免用户进入旧的占位执行页后产生两套流程。
- 报告页改为真实状态提示：未完成采样时禁止生成；完成采样后开放入口，但在计算与文件导出模块接入前明确提示，不伪造已生成报告。
- 现场巡检仪串口、Modbus 响应、CH1 数据和协议时序仍需连接真实设备复核；本轮只完成代码级验证，未将“编译通过”误认为“硬件通信通过”。

### 2026-08-18：参考 Qt 工作界面的校准工作台重排

- 参考 Qt 界面“大数据区 + 右侧操作区 + 底部执行区”的优点，重新调整 WPF 校准工作台：左侧扩大为实时趋势和测点数据矩阵，右侧固定为设备连接、当前任务和执行控制。
- 顶部摘要改为当前测量、稳定性、中心点、数据质量和最新采集五项固定信息，避免长文本挤在同一行造成重叠。
- 右侧控制区按设备连接、当前任务、任务参数、执行控制分层；已保存任务只显示摘要，任务参数统一回到“校准任务”页面修改，避免出现两套互相覆盖的参数表。
- 测点矩阵保留采集序号、采集时间和按温度/湿度点数联动生成的列，每行代表一组完整采集；趋势图保留最近 60 组数据，便于先观察稳定性再启动正式校准。
- 主窗口左侧导航增加“校准流程”和“数据与管理”分组提示，菜单顺序保持任务配置、校准工作台、校准结果、历史记录、系统设置。
- 增加当前校准任务的本机用户目录持久化；软件重启后自动恢复任务配置，但不恢复上一次采样完成状态，避免没有原始数据时错误开放结果流程。
- 本轮编译验证：WPF 主工程 0 个警告、0 个错误；真实 UI 尺寸和串口设备行为仍需在目标电脑连接巡检仪后现场复核。

### 2026-08-18：从用户视角重划任务配置与系统设置

- 原“校准任务”页只是任务流程概览，重复左侧菜单并增加一次无效点击；现在启动后和左侧“任务配置”直接打开实际配置表单，原 `TaskView` 保留为兼容代码但不再作为主流程入口。
- 系统设置职责收敛为长期资料：标准器名称、证书编号、有效期、型号、出厂编号、校准机构和标准器不确定度。
- 本次任务配置职责明确为现场任务：校准规范、被校设备、设备容积、校准类型、设定值、温湿度测点数、中心测点、现场环境温湿度和采样计划。
- 任务配置页增加“引用标准器”和“引用不确定度”信息卡，自动读取系统设置；需要修改长期资料时通过“维护系统资料”进入系统设置，避免两处编辑同一参数。
- 环境温度和环境湿度从系统设置移入当前任务配置，并随任务持久化，保证每份校准任务记录的是本次现场条件，而不是一个可能已经变化的全局默认值。
- 保存任务前增加现场环境数值范围检查：环境温度 -100～200 ℃，环境湿度 0～100 %RH。

### 2026-08-18：整体信息架构收敛为三项一级导航

- 主导航由“任务配置、校准工作台、校准结果、历史记录、系统设置”收敛为“校准作业、历史记录、系统设置”。任务配置、实时采集和结果判定改为校准作业内的步骤 1～3，避免用户越级进入结果页。
- 重写 `MainWindow` 应用外壳，删除废弃首页、动态首页卡片、串口测试和重复导航代码；所有一级菜单通过同一个选中状态方法切换，修复多个菜单同时高亮的问题。
- 校准工作台新增“查看结果与报告”按钮，仅在计划采样完成后启用；采集运行期间返回任务配置会提示先暂停，防止修改正在执行任务的参数。
- 正式校准采样使用独立计数器显示“当前组数 / 计划组数”，不再把稳定观察阶段的实时数据计入正式校准进度。
- 计划采样次数改为必须大于 0；JJF 1101-2019 默认 16 组、120 s，JJF 1376-2012 默认 20 组、180 s，用户仍可在任务配置中调整。
- 新增独立 `HistoryView`。当前归档存储模块尚未接入时显示真实空状态，不再让“历史记录”误打开当前任务报告页。
- 删除已无入口的 `TaskView` 和旧 `CalibrationView` 四个文件，避免维护人员继续扩展废弃流程。
- 保存任务时复制标准器身份、证书、有效期、型号、出厂编号、校准机构和不确定度为任务快照，确保后续系统设置变更不影响既有任务依据。
- 结果页增加任务规范、现场环境和标准器快照摘要；结果计算模块未接入前继续显示占位状态，报告生成按钮保持禁用。

### 2026-08-18：按 JJF 1101-2019 与 JJF 1376-2012 补全参数和规范流程

- 完整复核两份规范的适用范围、环境条件、测量标准、校准项目、校准点选择、布点方式、稳定条件、采样节拍、数据公式、原始记录和证书字段，并新增 `CalibrationStandardRuleService` 统一管理规范规则，避免页面和执行代码各写一套默认值。
- JJF 1101-2019 任务联动：设备容积不大于 2 m³ 时默认温度 9 点、湿度 3 点、温度中心点 5；大于 2 m³ 时默认温度 15 点、湿度 4 点、温度中心点 15；湿度中心位置 O 通过独立湿度中心通道记录，默认按 A/B/O 或 A/B/C/O 顺序映射到末通道，允许按实际接线调整。正式采样默认每 120 s 一组，共 16 组。
- JJF 1376-2012 任务联动：测温区容积不大于 0.15 m³ 时默认 5 点、中心监控点 3；大于 0.15 m³ 时默认 9 点、监控点 9。达到热稳定后每 180 s 一组，至少 20 组，并在界面提示每轮所有测点应在 1 min 内完成。
- 现场条件改为规范边界校验：两份规范均要求 15 ℃～35 ℃、湿度不大于 85 %RH；JJF 1101 额外要求气压 80 kPa～106 kPa。任务还必须确认无强烈振动、腐蚀性气体、强气流或其他冷热源等干扰；负载校准必须填写负载说明。
- 任务配置补全委托单位、地址、被校设备名称、制造厂、型号规格、出厂编号、使用范围、校准地点、校准日期、校准员、核验员、测温区尺寸、被校设备分辨力、布点方式与说明、偏离/客户要求说明等原始记录和证书必要字段。
- 规范默认布点时锁定测点数和中心点；选择实际工作位置或客户要求自定义时开放编辑并强制填写说明。JJF 1101 调整默认 16 组/120 s 时必须记录原因；JJF 1376 的 20 组/180 s 计划按规范锁定。
- 系统设置补全标准器量程、温湿度分辨力、准确度/MPE、测温仪器级别、热电偶等级、实验室地址、证书扩展不确定度 U 与包含因子 k、相邻校准修正值最大变化，以及温度 50 通道/湿度 10 通道的证书修正值。修正值采用 `通道号:修正值` 格式并在任务保存时固化快照。
- 实时采集收到数据后按任务快照应用各通道修正值，同时保留修正前数值、修正值和原始 HEX。标准器资料缺失、证书到期或箱式炉外观/运行检查未确认符合时，仍允许实时联调，但禁止启动正式校准。
- 原“最近 5 组达到固定阈值即稳定”的逻辑降级为趋势预检，不再冒充规范判据。JJF 1101 支持按说明书确认、到达设定值后等待 30～60 min、或人工确认提前开始；JJF 1376 必须人工确认达到校准温度并处于热稳定状态。
- 新增 `CalibrationRunContext`，将快速实时趋势数据与正式校准样本彻底分离。启动正式校准后，第一组立即留存，后续严格按任务中的 120 s/180 s 间隔取样，不再按 2 s 通讯刷新周期错误累计正式样本；每组同时保存被校设备温湿度示值。
- 新增规范结果计算：JJF 1101 计算温湿度上/下偏差、均匀度和波动度，并按附录 C 的重复性、分辨力、证书修正值和稳定性分量计算偏差扩展不确定度；JJF 1376 计算炉温均匀度、稳定度、偏差、炉内最大温差，并按附录 D 计算炉温均匀度扩展不确定度。结果页明确不把规范中的参考技术指标直接用于合格判定，也不把标准器证书 U 直接冒充最终测量不确定度。
- 验证结果：主工程 `dotnet build` 为 0 个警告、0 个错误；临时规则检查覆盖两种规范四种容积分支、通道修正格式及两套结果公式并全部通过。程序可启动并保持运行；当前 Windows 截图接口返回“不支持此接口”，因此页面像素级截图仍需在 Visual Studio 运行窗口中人工复核。真实串口采样节拍、证书到期拦截和 30 min 稳定计时必须连接实际巡检仪继续现场验证。

### 2026-08-18：取消任务实值硬编码并明确规范联动边界

- 新任务不再自动写入 `20 ℃`、`50 %RH`、`Pt100`、`0.1 ℃` 或 `1 %RH`。设定温湿度、传感器类型和被校设备分辨力均属于本次任务的实际信息，必须由操作人员按设备与现场填写。
- 切换校准规范时不再覆盖已经填写的设定温度；传感器类型不再由规范代选，保存前增加必选校验。
- 温度/湿度空间测点数、温度中心点、布点说明、正式样本数和采样间隔继续由 `CalibrationStandardRuleService` 按规范、容积和校准项目生成；规范默认布点时以浅灰只读样式显示，避免与人工实值混淆。
- 湿度 O 点是规范规定的空间位置，但 O 点接到巡检仪的哪个湿度通道取决于现场接线，因此“湿度 O 对应通道”始终允许修改；规则服务只提供 A/B/O 或 A/B/C/O 顺序下的建议值。
- 被校湿度分辨力为 `0` 的旧占位值不再回填为 `1`，而是显示为空并阻止保存，避免错误分辨力进入不确定度计算。已保存任务中的有效实值仍按任务记录回显。
- 新任务不再默认选择“小容积”分类；操作人员必须确认实际容积后，软件才生成对应的规范布点，避免错误地把 `9/3` 点或 `5` 点方案应用到大容积设备。
- 现场环境温度、湿度和气压取消 `25 ℃`、`50 %RH`、`101.3 kPa` 占位值，必须填写本次现场实测值并通过规范环境范围校验后才能保存任务。
- 修复任务配置页与校准工作台各自硬编码传感器列表导致的索引错位：原索引 `4` 在两页分别代表 S 型和 E 型。新增共享 `TemperatureSensorCatalog`，两页统一显示同一目录，并以稳定的传感器代码写入任务快照，后续调整显示顺序不会改变历史任务含义。

### 2026-08-18：修复实时采集全零、湿度字节序和测点矩阵联动

- 对照巡检仪 Modbus 协议、Qt 原程序及 COM7 真实响应复核采集链路。温度请求保持 `01 03 00 01 00 64`，每个主温度通道按 4 字节单精度浮点数解析；温湿度组合读取的两帧之间继续保留约 200 ms，避免连续请求使设备接收异常。
- 定位到矩阵全零的直接原因：湿度块中还包含 10 个“湿度探头伴随温度”，它们与主温度通道重复使用 CH1～CH10，原界面按“温度类型 + 通道号”刷新时用伴随温度的 0 覆盖了主温度。现在新增主温度、湿度、湿度探头伴随温度三种通道角色，矩阵、趋势、修正值和结果计算只使用本次任务要求的主温度与湿度通道。
- 修复湿度块单寄存器的设备字节序：现场原始值 `AC 02` 应按低字节在前解释为 `0x02AC × 0.01 = 6.84 %RH`，不再错误显示为负数或超量程；伴随温度仍作为诊断数据保留，但不计入校准测点。
- 测点矩阵列数与温度/湿度测点数实时联动；任务切换回工作台时会重新读取任务上下文并重建矩阵。采集运行期间锁定校准类型和测点数，防止中途修改造成表头与数据批次错位。
- 数据质量改为只统计本次任务要求的测点，明确显示“有效/要求”和异常数；缺失、设备特殊值或越界通道可通过提示查看通道号、状态和原始 HEX。设备特殊值 `-9999.900` 显示为“异常”，不再显示为正常数值或 0。
- 顶部摘要改为短行温度/湿度均值、范围、趋势、中心点和质量信息，降低窄窗口下的文字重叠；连接、实时测量、正式校准、停止和清空操作移到右侧控制区顶部，连接按钮可明确切换为“断开巡检仪”。
- 实时快照只保留最近 600 组用于趋势，矩阵只保留最近 200 行，正式校准样本仍由独立上下文完整保存，避免长时间运行导致 UI 集合无限增长。
- COM7 单次现场验证结果：主温度 CH1、CH2、CH4～CH9 均读取 `123.400 ℃`，CH3 正确识别设备特殊值 `-9999.900 ℃`；湿度 CH1～CH3 读取 `6.84 / 6.32 / 1.68 %RH`。协议解析、任务通道筛选、修正值和结果规则检查全部通过。

### 2026-08-18：补齐校准依据、当前校准点与空间布点联动

- 重新核对 JJF 1101-2019 第 7.2.1～7.2.3 条和 JJF 1376-2012 第 7.3.1～7.3.3 条，明确区分“校准点（设备设定温湿度）”与“布点（工作空间/测温区中的传感器位置）”，避免界面把两类概念混为同一个“点数”。
- 修复原“校准点选择”仅保存索引、没有事件和业务作用的问题。切换校准依据后，校准点方案随规范变化：JJF 1101 提供用户常用点、使用范围下限/上限/中间点、客户指定点；JJF 1376 提供实际常用温度、最低和最高工作温度、客户指定温度。
- 规范中的“校准点方案”可能包含多个工况，当前执行引擎仍以“一个任务对应一个当前校准工况”执行。因此界面不再把范围方案写成“任选一点”：JJF 1101 下限、上限和中间点需逐工况完成；JJF 1376 最低和最高工作温度需逐工况完成。设定温度/湿度作为当前工况实值，不由规范写死；系统从“使用/测量范围”解析上下限并校验当前值，客户指定点必须填写客户要求说明。
- JJF 1101 空间布点按容积联动：不大于 2 m³ 时温度 9 点、湿度 3 点、温度中心点 5；大于 2 m³ 时温度 15 点、湿度 4 点、温度中心点 15。默认说明补充各点距内壁为相应边长 1/10、受风道影响时不超过 500 mm，以及小于 0.05 m³、超过 50 m³和带样品架时可按规范调整的条件。
- JJF 1101 提供“按实际工作位置调整”和“极端容积按需调整点数”两种自定义方式。根据现场使用要求，“按实际工作位置调整”同样开放温度点数、湿度点数和中心点编辑，并强制填写布点说明；“极端容积”模式继续校验实际容积必须小于 0.05 m³ 或大于 50 m³。湿度 O 对应巡检仪通道仍按现场接线确认。
- 工作/测温区长宽高与容积档位建立校验：JJF 1376 必须填写尺寸，JJF 1101 填写任一尺寸后必须完整填写三项；计算容积若与 ≤/> 2 m³ 或 ≤/> 0.15 m³ 档位不一致，禁止保存，避免“容积选项与布点矩阵不一致”。
- JJF 1376 的布点方式改为“按工作区尺寸确定测温区”或“按炉膛尺寸按图 2 确定测温区”。两种方式只改变测温区确定依据，不开放任意点数：不大于 0.15 m³ 强制 5 点、中心监控点 3；大于 0.15 m³ 强制 9 点、监控点 9。
- 切换规范但尚未选择容积时，旧规范的湿度字段、气压字段、空间点数和布点说明会立即清理或隐藏，不再残留上一规范的数据；选择容积后才生成新的空间点数和中心点。
- 任务配置页新增“依据 - 校准点 - 空间布点联动”摘要，实时显示规范、校准项目、当前设定点、容积、布点方式、温湿度空间点数和中心点。保存任务后，校准工作台继续按这些点数生成实时数据矩阵。
- 验证结果：WPF 主工程构建 0 个错误；当前编译警告均来自本次范围外的未跟踪文件 `Communication/test.cs`。规则检查新增 JJF 1101 校准点策略、自定义布点边界，以及 JJF 1376 最低/最高温度与工作区/炉膛测温区分支，全部通过。

### 2026-08-18：修复正式校准按钮阻塞原因不清和标准器资料流程断点

- 确认实时测量值等于任务设定值只表示设备已到达当前工况，不能单凭一个瞬时值代替规范要求的稳定状态确认。按设备说明书/现场状态判定的任务仍需操作人员勾选“已确认稳定”；按默认等待判定的任务从操作人员确认到达设定值后开始计时。
- 原页面只返回第一个阻塞条件且将“启动校准采样”按钮直接置灰，用户无法判断还有哪些条件未完成。现在一次显示全部阻塞项，并在趋势已稳定时明确提示“最近 5 组波动已稳定，但仍需完成规范确认”。
- 校准按钮根据当前首要问题显示“请先完善标准器资料”“请先确认设备稳定”或“等待有效实时数据”，鼠标提示保留完整阻塞清单，不再出现无解释的灰色按钮。
- 修复任务配置流程断点：新任务保存前必须验证标准器名称、证书编号和有效期，证书过期时禁止进入工作台；不再允许先建立不完整任务、到正式采样时才发现无法继续。
- 为已有任务增加“同步/维护标准器资料”入口。系统设置中的标准器资料完整且有效时，可显式同步到当前任务并继续实时测量；资料仍不完整时可直接前往系统设置维护，返回校准作业后再次同步。同步动作不会清空实时测量数据。

### 2026-08-18：被校设备与委托信息改为可选档案模式

- “被校设备与委托信息”中的委托单位、校准地点、设备名称、制造厂、型号规格、出厂编号、校准日期、委托单位地址、校准员和核验员全部改为可选，不再参与任务保存前的统一必填校验。
- 页面明确提示这些内容可在进入实时测量后、出具报告前按需要补充，减少设备联调和现场快速测量时的无关输入。
- “使用/测量范围”从委托信息移入“校准点与布点”：通常可留空，仅当选择范围下限/上限/中间点方案时才作为联动参数必填。
- “工作/测温区尺寸”从委托信息移入“校准点与布点”：JJF 1376 测温区布点以及 JJF 1101 极端容积调整点数时仍按规范条件必填，其他 JJF 1101 任务可留空。
- 工作台和结果页增加空档案占位说明。未填写设备名称、型号和编号时显示“设备档案未填写（可选）”，不再出现空括号或空分隔符。
